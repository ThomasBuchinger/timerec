---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: timerec-api
  namesapce: default
  labels:
    app.kubernetes.io/name: timerec
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: timerec
  serviceName: timerec-api
  replicas: 1
  template:
    metadata:
      labels:
        app.kubernetes.io/name: timerec
    spec:
      serviceAccountName: timerec-sa
      containers:
      - name: timerec-server
        image: ghcr.io/thomasbuchinger/timerec:main
        imagePullPolicy: Always
        env:
          - name: WATCH_NAMESPACE
            valueFrom:
              fieldRef:
                fieldPath: metadata.namespace
        ports:
        - name: http
          containerPort: 8080
        resources:
          requests:
            memory: "10Mi"
            cpu: "1m"
          limits:
            memory: "20Mi"
            cpu: "10m"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          failureThreshold: 3
          periodSeconds: 5
          successThreshold: 1
          timeoutSeconds: 60
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8080
          failureThreshold: 3
          periodSeconds: 5
          successThreshold: 1
          timeoutSeconds: 60
        volumeMounts:
        - name: config
          mountPath: /
      volumes:
        - name: config
          configMap:
            name: timerec-cm

